<h1>Students</h1>
<% @students.each do |student| %>
  <h3><%= "#{student.first_name} #{student.last_name}" %></h3>
  <p>
    <%= link_to 'View Student', student_path(student) %>
  </p>
<% end %>
<%= link_to 'New Student', new_student_path %>